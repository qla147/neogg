const utils = require("../../common/utils/utils")
const ErrorCode = require("../../common/const/ErrorCode")


const service = {}



service.get = (userInfo , params)=>{
    try{
        const { pageSize , pageNo , }
    }catch (err) {
        console.error(er)
        return utils.Error(err)
    }


}

module.exports = service